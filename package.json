{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^4.1.4",
    "jsdom": "^26.1.0",
    "@vitest/coverage-v8": "^3.1.4"
  }
}
